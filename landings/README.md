# Landings

Each folder in this directory is intended to be a self-contained landing page project.

Use a numeric prefix to keep the collection ordered:

```txt
001-project-name/
002-project-name/
```
